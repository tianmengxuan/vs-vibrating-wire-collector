# 2026-05-06 — Flutter 开发环境安装

## 完成内容
- 自动安装 Flutter SDK 3.41.5 + Android SDK 全套开发环境

## 安装位置
- Flutter SDK: D:\dev\flutter
- Git: D:\dev\Git
- JDK 17: D:\dev\Java\jdk-17.0.14+7
- Android SDK: D:\dev\Android

## 环境变量配置（系统级别 - User）
- PATH 添加: D:\dev\flutter\bin, D:\dev\Git\bin, D:\dev\Java\jdk-17.0.14+7\bin, D:\dev\Android\cmdline-tools\latest\bin, D:\dev\Android\platform-tools
- ANDROID_HOME = D:\dev\Android
- JAVA_HOME = D:\dev\Java\jdk-17.0.14+7
- FLUTTER_STORAGE_BASE_URL = https://mirrors.tuna.tsinghua.edu.cn/flutter
- PUB_HOSTED_URL = https://mirrors.tuna.tsinghua.edu.cn/dart-pub

## 注意
- 需重新打开终端/VS Code 使环境变量生效
- 首次打开 VS Code 后，VS Code 的 Flutter 插件应能自动识别 D:\dev\flutter
- 如果 VS Code 识别不到，手动指定 Flutter SDK 路径为 D:\dev\flutter

---

## data_sensor 表集成
- **需求**: 解算软件部署到服务器后，将解算数据写入 rtu 库的 data_sensor 表
- **config.py**: DB_NAME 默认值改为 `rtu`
- **models.py**: 新增 `DataSensor` ORM 模型，映射 data_sensor 表
- **crud.py**: 新增 `insert_data_sensor()` 方法
- **server.py**: 在 `_on_device_data()` 解算步骤后集成 data_sensor 写入
- **数据映射**: sensor=f"{udid}CH{channel}", data1=频率, data2=温度, data3=解算值, batteryVolt=电压, dataSequence=数据包ID, CollectionTime=采集时间

## 数据库配置修正
- **DB_PASSWORD** → `jinpu123!@#`
- **DB_NAME** → `rtu`（data_sensor 表所在库）
- **密码URL编码**: `get_database_url()` 添加 `quote_plus()` 编码，防止 `@#` 特殊字符导致 URL 解析错误
- **2次打包**: 修正中文路径编码问题，最终 exe 26.3 MB
